{
  "num_train_epochs": 3,
  "learning_rate": 5e-5,
  "per_device_train_batch_size": 4,
  "logging_steps": 100,
  "save_steps": 500,
  "log_level": "INFO",
  "use_gpu": true,
  "gradient_accumulation_steps": 1,
  "max_length": 512,
  "early_stopping": true,
  "early_stopping_patience": 2
}

